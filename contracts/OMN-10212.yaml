---
schema_version: "1.0.0"
ticket_id: OMN-10212
title: "Gate 1 identity field optionality"
summary: "Gate 1: ban optional defaults for identity field names"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Substrate gate unit tests pass"
    command: "uv run pytest tests/unit/cli/substrate_gates/ -q"
  - kind: ci
    description: "omnibase_core PR 969 checks pass"
    command: "gh pr checks 969 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Identity optionality gate is present in PR 969"
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr diff 969 --repo OmniNode-ai/omnibase_core | grep -q 'identity_field_optionality'"
  - id: dod-002
    description: "Deploy-gate evidence: substrate gate is a static pre-commit CLI check, not a runtime
      service"
    source: manual
    checks:
      - check_type: command
        check_value: "echo 'deploy-gate: OMN-10212 changes only substrate_gates static analysis code;
          no Docker image, runtime process, broker topic, or service deploy is required'"
