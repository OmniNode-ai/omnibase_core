---
schema_version: "1.0.0"
ticket_id: OMN-10217
title: "Gate 4 silent except pass"
summary: "Gate 4: ban silent except pass handlers"
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements:
  - kind: tests
    description: "Substrate gate unit tests pass"
    command: "uv run pytest tests/unit/cli/substrate_gates/ -q"
  - kind: ci
    description: "omnibase_core PR 966 checks pass"
    command: "gh pr checks 966 --repo OmniNode-ai/omnibase_core"
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: dod-001
    description: "Silent except pass gate is present in PR 966"
    source: manual
    checks:
      - check_type: command
        check_value: "gh pr diff 966 --repo OmniNode-ai/omnibase_core | grep -q 'silent_except_pass'"
  - id: dod-002
    description: "Deploy-gate evidence: substrate gate is a static pre-commit CLI check, not a runtime
      service"
    source: manual
    checks:
      - check_type: command
        check_value: "echo 'deploy-gate: OMN-10217 changes only substrate_gates static analysis code;
          no Docker image, runtime process, broker topic, or service deploy is required'"
