# Active repos requiring architecture-handshake compliance.
# Format: one repo name per line (must match the GitHub repo name exactly).
# Blank lines and #-comments are ignored.
# Used by: install.sh (supported repos list), check-policy-gate.sh (compliance checks).
omnibase_core
omnibase_infra
omnibase_spi
omniclaude
omnidash
omniintelligence
omnimemory
omninode_infra
