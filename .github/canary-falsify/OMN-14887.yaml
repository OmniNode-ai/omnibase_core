---
# OMN-14887 falsification fixture (TEMPORARY -- proof-of-decouple only).
#
# This file exists for exactly one CI run: with the contract-compliance job's
# --contracts-dir temporarily pointed at this directory (see the paired ci.yml
# change in this same commit), run_contract_compliance_check.py resolves the
# ticket id from this PR's title/branch/body (OMN-14887), loads
# .github/canary-falsify/OMN-14887.yaml as the contract, and executes the
# single command check below. `exit 1` is a non-inert, hermetic, guaranteed
# BLOCK -- it does not touch the OCC receipt/contract store (so it is not
# demoted INERT) and does not shell out to ssh/docker/network (so it is not
# demoted non-hermetic). The expected live result is:
#   Contract Compliance Check = FAILURE
#   CI Summary                = FAILURE (still merge-blocking)
#   Tests (Split N/M)         = EXECUTED, not skipped
# proving contract-compliance's removal from quality-gate's `needs:` actually
# decouples merge-blocking from test-suppression. The very next commit on this
# branch reverts this file and the paired ci.yml override.
schema_version: "1.0.0"
ticket_id: "OMN-14887"
title: "CANARY FALSIFICATION FIXTURE -- not a real ticket contract"
summary: >
  Temporary fixture proving contract-compliance can BLOCK while
  detect-changes/test-parallel still execute. Reverted in the following
  commit on this branch.
is_seam_ticket: false
interface_change: false
interfaces_touched: []
evidence_requirements: []
emergency_bypass:
  enabled: false
  justification: ""
  follow_up_ticket_id: ""
dod_evidence:
  - id: "canary-falsify-guaranteed-block"
    description: "Guaranteed-failing, non-inert, hermetic command check to prove the gate can BLOCK."
    source: "manual"
    checks:
      - check_type: "command"
        check_value: "exit 1"
