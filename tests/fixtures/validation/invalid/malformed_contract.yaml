contract_version:
  major: 1
  minor: 0
  patch: 0
# Missing required node_type field

metadata:
  name: "IncompleteContract"
  version:
    major: 1
    minor: 0
    patch: 0

inputs:
  - name: "input_data"
    type: "object"
    # Missing required field
    schema:
      type: object  # Malformed YAML - missing quotes

outputs
  # Malformed YAML - missing colon
  name: "output_data"
  type: "object"
